---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ts-bridge-pv-claim
spec:
  storageClassName: ssd-backed
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
